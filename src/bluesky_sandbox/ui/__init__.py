"""Things people look at.

The environment designer (:mod:`.designer`), the render backends
(:mod:`.drivers`), and the driver-independent drawing vocabulary they
consume (:mod:`.display`).

Importless by design - see :mod:`bluesky_sandbox.sim`.
"""
